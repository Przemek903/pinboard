$('#pins').append('<%= j render partial: "pins/pins", object: @pins %>');
<% if @pins.next_page %>
$('.pagination').replaceWith('<%= j will_paginate @pins  %>');
<% else %>
$('.pagination').remove();
<% end %>